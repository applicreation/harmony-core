---

auth:
  token:
    secret: ${{AUTH_TOKEN_SECRET}}
    ttl: ${{AUTH_TOKEN_TTL}}
  google:
    client_id: ${{AUTH_GOOGLE_CLIENT_ID}}
github:
  token: ${{GITHUB_TOKEN}}
modules:
  - id: catalog
  - id: docs
  - id: test-one
  - id: test-two
  - id: test-three
  - id: test-four
  - id: test-five
  - id: test-six
